# S0 symbolic baseline benchmark

Status: PENDING GitHub Actions development benchmark.

The benchmark result will be recorded after CI executes the frozen 60-task public evaluation development split.
