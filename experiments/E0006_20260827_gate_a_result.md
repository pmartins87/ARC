# E0006 — Gate A Kaggle environment result

Date: 2026-08-27
Status: **ENVIRONMENT PASS / DEPENDENCY BLOCKED**

## Observed Kaggle run

The saved Kaggle L4 x4 run completed and produced `e0006_gate_a_inspect.json`.

Observed environment:

- 4 × `NVIDIA L4` visible;
- CUDA capability: 8.9 on all four GPUs;
- PyTorch-reported memory per GPU: **22.034 GiB**;
- Python: **3.12.13**;
- torch: **2.10.0+cu128**;
- CUDA runtime seen by torch: **12.8**;
- transformers: **5.0.0**;
- accelerate: **1.13.0**;
- safetensors: **0.7.0**;
- triton: **3.6.0**;
- vLLM: **not installed**;
- FlashInfer: **not installed**;
- working disk: **19.518 GiB total / 19.502 GiB free**.

Attached model:

- architecture: `NemotronHForCausalLM`;
- model type: `nemotron_h`;
- 52 safetensor shards;
- mounted checkpoint size: **20.101 GiB**;
- chat template present;
- mounted fully under `/kaggle/input/models/.../30b-a3b-nvfp4/1`.

## Gate-A status correction

The first notebook emitted `BLOCKED_GATE_A` solely because the check expected `>=23.5 GiB` while treating NVIDIA's marketing **24 GB** figure as though it were **24 GiB**. That criterion was wrong.

The actual run exposes four genuine NVIDIA L4 devices with SM 8.9. `22.034 GiB` reported usable total memory is approximately `23.66 GB` in decimal units and is consistent with the nominal 24 GB class after binary/decimal conversion and runtime-visible reservation differences.

Therefore the hardware/model-attachment portion of Gate A is reclassified **PASS**.

## Real blocker discovered

Gate B must **not** run from the current Kaggle base image because both `vllm` and `flashinfer` are absent.

This is exactly the high-information result Gate A was intended to expose. Installing from the Internet during the competition-valid L4 run is forbidden by project protocol and by the offline evaluation regime.

Next action:

1. prepare and validate an offline dependency bundle without L4 GPU;
2. attach that bundle as a Kaggle input;
3. run a dependency/import probe before any model load;
4. only then release the bounded TP4 Gate B.

No additional L4 run is authorized until the offline dependency path is frozen.
