# GitHub Actions usage

During M1, Actions provides low-cost deterministic validation of CPU-side research artifacts. It is not treated as a substitute for Kaggle's competition runtime. Continuous jobs may use only the evaluation-development split; validation and held-out runs remain explicit milestone gates.
