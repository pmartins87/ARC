Current milestone: **M1 — Baselines / feasibility**, due 2026-09-02.

- N1 `ARC2 vanilla exact`: **PASS / COMPLETE**. Kaggle Version 1 rerun succeeded with **Public Score 29.72**. Source snapshot reference was **31.39**, so reproduction delta is **-1.67pp**. N1 is now our end-to-end Kaggle-valid neural baseline anchor; do not spend more submissions rerunning a correlated public family for score alone.
- S0 shallow symbolic: **REJECT standalone** (0/82 exact dev outputs).
- Candidate-pool, selector, diversity, runtime/coverage and dataset-provenance instrumentation: **READY on main**.
- Public-code frontier refresh: `Failed in AIMO` is now the strongest public notebook score verified in our audit at **33.89**, with a successful **26m11s L4 x4** run and **44 output files**. It is assigned **P33** and is **INSPECT-ONLY first**; no score-only reproduction is authorized. Audit: `docs/M1_PUBLIC_3389_AUDIT.md`.
- A related public clone exposes `qwen3_4b_bfloat16-v02-01-01`, `flash-attn-2.8.2` and an Unsloth dependency but scores 29.86. Treat this as family/provenance evidence, not proof of the mechanism behind 33.89.
- Live-prize frontier remains a separate regime: latest frozen organizer/public evidence has leaders at ~70%+; M2 must seek step-change leverage rather than incremental public-notebook tuning. The move from 31.81 to 33.89 does not change that conclusion.
- Evaluation protocol correction: E0007/E0008 found the original hash split's untouched gates structurally skewed (validation median test-input area **642.5** vs heldout **285**). Before any Level C/D score was used, we preserved the 60-task development set exactly and rebalanced only the untouched 60-task gate pool using training examples + test inputs. `experiments/evaluation_split_v2.json` is now the immutable validation/heldout manifest. Its median test-input areas are **473.5 / 484** for validation/heldout and multi-test fractions **36.7% / 40.0%**. No test output was read.
- N2 correlated ~31% TRM sibling: **SKIP by default**; launch only if a concrete complementarity/provenance question later justifies it and P33 does not already expose the required artifact evidence.
- **Current external gate:** E0006 — Nemotron 3.5 Lightning + NVIDIA NeMo Gym NVARC L4 x4 feasibility smoke. Official public sources provide ARC-specific post-training disclosure, an Apache-2.0 exact verifier with direct-grid and executable-`transform()` modes, vLLM support and TP=4 serving references.
- E0006 source checkpoint is frozen to HF revision **`cc84af2fe71647d87f4486c064f320e1e7535243`** because the upstream model repository is actively changing. Provenance is recorded in `docs/E0006_MIRROR_PROVENANCE.md`.
- The direct Hugging Face -> Kaggle starter-notebook materialization route is **REJECTED for this checkpoint** after two bounded attempts.
- Cloud mirror fallback is **PASS / MIRROR_READY**. `paulomartins87/nemotron-3-5-lightning/pyTorch/30b-a3b-nvfp4` Version 1 mounts successfully under `/kaggle/input/models/...` and exposes the pinned checkpoint offline.
- **Gate A run completed on Kaggle L4 x4. Hardware/model attachment PASS.** Four genuine `NVIDIA L4` devices are visible, all capability **8.9**, with PyTorch reporting **22.034 GiB** each. The attached checkpoint is **20.101 GiB**, architecture `NemotronHForCausalLM`, model type `nemotron_h`, with **52 safetensor shards** and chat-template metadata present.
- Gate A's first JSON said `BLOCKED_GATE_A` only because the notebook incorrectly compared NVIDIA's nominal **24 GB** capacity against a **23.5 GiB** threshold. That binary/decimal criterion is invalid. `22.034 GiB` is approximately **23.66 GB decimal** and the device identity/capability confirms the intended L4 class. The experiment is reclassified **ENVIRONMENT PASS**. Result: `experiments/E0006_20260827_gate_a_result.md`.
- **Real Gate A blocker:** the Kaggle base image has **no `vllm` and no `flashinfer` installed**. Observed L4 stack: Python **3.12.13**, torch **2.10.0+cu128**, CUDA runtime **12.8**, transformers **5.0.0**, accelerate **1.13.0**, safetensors **0.7.0**, triton **3.6.0**. Working disk was **19.502 GiB free**.
- No-GPU dependency dry run is complete. Generic `vllm[flashinfer]==0.27.1` resolution returned successfully but planned **105 installs/changes**, including torch **2.13.0**, transformers **5.16.1**, Triton **3.7.1**, FlashInfer **0.6.16.post3** and a broad CUDA 13.x package family. That generic PyPI installation path is **REJECTED** for the competition-valid runtime. Result: `experiments/E0006_20260827_dependency_probe_result.md`.
- The official vLLM v0.27.1 release publishes a dedicated Linux x86_64 **CUDA 12.9** wheel, `vllm-0.27.1+cu129-cp38-abi3-manylinux_2_28_x86_64.whl`, digest `bf0d52faa2a51e7a01c6856a7a8a2d1307fd0ff711415d34168a67ffac0fa47b`. Official vLLM guidance pairs CUDA-specific wheels with the matching PyTorch CUDA index, and PyTorch publishes `torch 2.13.0+cu129` for CPython 3.12 Linux x86_64.
- Therefore **Gate B remains NOT AUTHORIZED**. Next step is still zero-L4: run `notebooks/E0006_vllm_cu129_resolution_probe_kaggle.ipynb` with Accelerator None / Internet ON. It resolves the exact `+cu129` wheel against the PyTorch cu129 and FlashInfer indexes and estimates the planned wheelhouse size before any bundle is built.
- Only after the exact cu129 dependency plan is reviewed may a private offline wheelhouse/runtime input be built. That runtime must pass an offline import probe before any TP4 model-load run consumes L4 quota.
- NVIDIA's model card for this checkpoint identifies **vLLM 0.27.1** as its validated vLLM release. Current vLLM documentation supports `NemotronHForCausalLM` and ModelOpt NVFP4, with W4A16 fallback on non-native-FP4 GPUs; exact Kaggle offline dependency compatibility remains to be measured rather than assumed.
- NVIDIA's public NeMo Gym NVARC compatibility verifier is **READY on main** for both transductive direct-grid extraction and inductive executable `transform()` verification, with exact binary reward and a guarded local probe path.
- NVIDIA's official `Nemotron-RL-ARC-AGI-v1` gives a source-faithful public prompt/reward baseline for both NVARC modes. Dataset metadata still says `pending-legal-review`, so checkpoint-only evaluation is cleaner than new training on those bytes.
- E0006 model-weight license is **OpenMDW-1.1**. Feasibility/development testing can continue, but prize eligibility of a future official submission materially dependent on these weights is an explicit unresolved gate in `docs/E0006_LICENSE_GATE.md`; preserve upstream license/notices and do not relabel the weights.
- E0006: **MIRROR_READY -> GATE_A_ENV_PASS -> GENERIC_RUNTIME_REJECTED -> CU129_RESOLUTION_PROBE**. No further L4 run is authorized until the offline runtime path is validated.
- Paper Prize path remains active from the same evidence stream. Current organizer pages disagree by one day: Kaggle submission host says **2026-11-09 23:59 UTC**, ARC Prize overview says Nov 8. Project rule: operational external deadline Nov 9, **internal safety deadline Nov 8** until re-verified.
- Before any original competitive mechanism is committed publicly, trigger `docs/VISIBILITY_GATE.md`.
