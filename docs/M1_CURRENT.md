Current milestone: **M1 — Baselines / feasibility**, due 2026-09-02.

- N1 `ARC2 vanilla exact`: **PASS / COMPLETE**. Kaggle Version 1 rerun succeeded with **Public Score 29.72**. Source snapshot reference was **31.39**, so reproduction delta is **-1.67pp**. N1 is now our end-to-end Kaggle-valid neural baseline anchor; do not spend more submissions rerunning the same/correlated ~30–32% family for score alone.
- S0 shallow symbolic: **REJECT standalone** (0/82 exact dev outputs).
- Candidate-pool, selector, diversity, runtime/coverage and dataset-provenance instrumentation: **READY on main**.
- Public-code frontier refresh: the strongest public-notebook score currently verified in our audit is **31.81** (`ARC Baseline Rebuild`, best Version 73). The small public-code delta over N1 does not justify a leaderboard-only run.
- Live-prize frontier remains a separate regime: organizer-reported leaders are ~70%+; M2 must seek step-change leverage rather than incremental public-notebook tuning.
- Evaluation protocol correction: E0007/E0008 found the original hash split's untouched gates structurally skewed (validation median test-input area **642.5** vs heldout **285**). Before any Level C/D score was used, we preserved the 60-task development set exactly and rebalanced only the untouched 60-task gate pool using training examples + test inputs. `experiments/evaluation_split_v2.json` is now the immutable validation/heldout manifest. Its median test-input areas are **473.5 / 484** for validation/heldout and multi-test fractions **36.7% / 40.0%**. No test output was read.
- N2 correlated ~31% TRM sibling: **SKIP by default**; launch only if a concrete complementarity/provenance question later justifies it.
- **Current external gate:** E0006 — Nemotron 3.5 Lightning + NVIDIA NeMo Gym NVARC L4 x4 feasibility smoke. Official public sources provide ARC-specific post-training disclosure, an Apache-2.0 exact verifier with direct-grid and executable-`transform()` modes, vLLM support and TP=4 serving references.
- Deployment evidence: NVIDIA's current NIM support matrix reports the Lightning **BF16 TP4** profile floor as **20 GB/GPU, minimum 4 GPUs, Ampere-or-newer / SM 8.0+**. NVIDIA lists L4 as **24 GB, Ada Lovelace, compute capability 8.9**, so Kaggle L4 x4 passes the published profile floor with nominal **4 GB/GPU** headroom. The preferred attachment is the **NVFP4 checkpoint**, expected to execute on L4 through the documented W4A16 fallback path and provide materially more headroom. This still does not prove bare-vLLM Kaggle compatibility or sufficient throughput.
- E0006 source checkpoint is now frozen to HF revision **`cc84af2fe71647d87f4486c064f320e1e7535243`** because the upstream model repository is actively changing. Provenance is recorded in `docs/E0006_MIRROR_PROVENANCE.md`.
- The direct Hugging Face -> Kaggle starter-notebook materialization route is **REJECTED for this checkpoint** after two bounded attempts: even after a successful tiny HF download and Save Version, `Add Input` still exposed only the user's notebook rather than an attachable model.
- Cloud mirror fallback reached the critical path successfully: the pinned source snapshot reconstructed to approximately **21.6 GB**, the Kaggle model family `paulomartins87/nemotron-3-5-lightning` was created, and a `30b-a3b-nvfp4` upload began. The local Chrome frontend later hit an `Out of Memory` page while rendering verbose progress; server-side model-version completion is **UNCONFIRMED**, not failed.
- A resilient mirror retry notebook is **READY on main**: `notebooks/E0006_mirror_nvfp4_to_kaggle.ipynb`. It pins the source revision, validates scratch space and a file manifest, runs upload in a child process, redirects detailed progress to a local log, emits sparse heartbeats and writes compact status/manifest JSON. Use it only if the first model version is not ready.
- Current Kaggle notebook-resource behavior gives us a lower-friction attachment fallback after a ready mirror: in a draft session, `kagglehub.model_download(<handle>, path="config.json")` attaches the model datasource and returns the mounted path. Datasource attachment must happen before `Save Version`.
- The offline no-leaderboard L4 **Gate A** inspect notebook is **READY on main**: it verifies 4×L4, local checkpoint attachment, package versions, disk and offline visibility without loading the model.
- The standalone **Gate B** notebook is also **READY on main**, but remains blocked behind Gate A review. It performs one bounded TP4 vLLM load + one short generation, captures structured failure classes/startup/memory/throughput evidence, writes JSON/log and terminates the server.
- NVIDIA's public NeMo Gym NVARC compatibility verifier is **READY on main** for both transductive direct-grid extraction and inductive executable `transform()` verification, with exact binary reward and a guarded local probe path.
- NVIDIA's official `Nemotron-RL-ARC-AGI-v1` gives a source-faithful public prompt/reward baseline for both NVARC modes. Dataset metadata still says `pending-legal-review`, so checkpoint-only evaluation is cleaner than new training on those bytes.
- E0006: **PENDING_MIRROR_VERIFY -> GATE_A**. No competition submission or Ryzen work has been launched for it.
- Paper Prize path remains active from the same evidence stream. Current organizer pages disagree by one day: Kaggle submission host says **2026-11-09 23:59 UTC**, ARC Prize overview says Nov 8. Project rule: operational external deadline Nov 9, **internal safety deadline Nov 8** until re-verified.
- Before any original competitive mechanism is committed publicly, trigger `docs/VISIBILITY_GATE.md`.
